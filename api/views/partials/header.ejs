<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <div <% if (pageName==='home' ) { %>class="active"
            <% } %>>
                <a class="navbar-brand" href="/">Journal de Classe</a>
        </div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item <% if (pageName==='sheet' ) { %>active
                    <% } %>">
                    <a class="nav-link pr-3" href="/sheets">Journal</a>
                </li>
                <li class="nav-item <% if (pageName==='teacher' ) { %>active
                    <% } %>">
                    <a class="nav-link pr-3" href="/teachers">Professeurs</a>
                </li>
                <li class="nav-item <% if (pageName==='lesson' ) { %>active
                    <% } %>">
                    <a class="nav-link pr-3" href="/lessons">Cours</a>
                </li>
            </ul>
        </div>
    </div>
</nav>